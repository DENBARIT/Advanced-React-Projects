reducer=>previous state and action as an argument and return the next state 
dispatch:to send from the event handler[ ex- const [state ,dispatch]] to the reducer,
=>we use  dispatch in the eventlistener to dispatch  an action to the reducer function,
Why reducer? It is used to accumulate actions overtime(to reduce similar to the array.reduce method) 
#fixations
=>1)previous question and next question=>add previous button 
2)add a filter difficult,easy question
3)for the answers to keep the highscore in the api so that for that to keep the highscore through out mounting
